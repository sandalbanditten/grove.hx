# TODO

## Finish `LS_COLORS` impl: some files are not the same color, and src/ dirs are yellow

## Use the same icons as `eza`

## Start with current path unfolded

## Start with adjusted width, essential `=` on startup

## Add a keybind for `spc` which does the same as `enter`


